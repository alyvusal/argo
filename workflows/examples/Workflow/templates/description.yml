  # A container set allows you to run multiple containers in a single pod.
  # This is useful when you want the containers to share a common workspace, or when you
  # want to consolidate pod spin-up time into one step in your workflow.

  # A data template allows you get data from storage (e.g. S3). This is useful when each item
  # of data represents an item of work that needs doing.
