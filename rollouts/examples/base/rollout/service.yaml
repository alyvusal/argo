apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-app
    tier: front
  name: demo-app
spec:
  ports:
  - name: "3000"
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: demo-app
  type: ClusterIP
