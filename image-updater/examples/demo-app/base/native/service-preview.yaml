apiVersion: v1
kind: Service
metadata:
  name: demo-app-image-updater-preview
  labels:
    app: demo-app-image-updater
    tier: front
spec:
  ports:
  - name: "3000"
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: demo-app-image-updater
  type: ClusterIP
