apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-app-image-updater
    tier: front
  name: demo-app-image-updater
spec:
  ports:
  - name: "3000"
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: demo-app-image-updater
  type: ClusterIP
